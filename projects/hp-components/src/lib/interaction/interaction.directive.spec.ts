import { InteractionDirective } from './interaction.directive';

describe('InteractionDirective', () => {
  it('should create an instance', () => {
    const directive = new InteractionDirective();
    expect(directive).toBeTruthy();
  });
});
