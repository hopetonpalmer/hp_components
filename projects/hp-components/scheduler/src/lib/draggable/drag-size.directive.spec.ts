import { DragSizeDirective } from './drag-size.directive';

describe('DragSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new DragSizeDirective();
    expect(directive).toBeTruthy();
  });
});
