import { ColorSchemeDirective } from './color-scheme.directive';


describe('ColorSchemeDirective', () => {
  it('should create an instance', () => {
   // const directive = new ColorSchemeDirective();
    // expect(directive).toBeTruthy();
  });
});
